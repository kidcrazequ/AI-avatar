---
description: '@soul/core 工具函数使用指南 — 修改 packages/core 代码时参考'
globs:
  - "packages/core/src/**/*.ts"
alwaysApply: false
---

# @soul/core 工具函数规范

修改 `packages/core` 代码时必须遵循。

## 已有工具函数（禁止重复实现）

| 函数 | 文件 | 用途 |
|------|------|------|
| `localDateString()` | `utils/common.ts` | 本地时区 YYYY-MM-DD |
| `collectFilesRecursive()` | `utils/common.ts` | 带深度限制的递归文件收集 |
| `assertSafeSegment()` | `utils/path-security.ts` | 路径段安全校验 |
| `resolveUnderRoot()` | `utils/path-security.ts` | 目录穿越防护 |
| `fetchWithTimeout()` | `utils/http.ts` | 带超时和错误分类的 fetch |

## 添加新工具函数的条件

1. 相同逻辑在 **2 个以上**模块中出现
2. 属于**横切关注点**（日期、路径、HTTP、日志等），而非领域逻辑
3. 函数是**纯函数**或仅依赖 Node.js 内建模块，不依赖 Electron

## 新工具函数清单

添加到 `utils/` 后，必须：
1. 在 `index.ts` 中 `export`
2. 添加完整 JSDoc（含参数说明和使用示例）
3. 在文件头标注 `@author` 和 `@date`
