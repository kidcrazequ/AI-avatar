# 电气工程师 — PDF/HTML 文档模板

> **作者**：zhi.qu  
> **日期**：2026-05-09  

本分身调用 `generate_document` 时可选用下列 CSS（桌面端会与内置基础样式叠加）。

## 模板清单

| templateName | 适用场景 |
|--------------|----------|
| `default` | 设计说明摘录、自检清单、验收纪要 |

## 用法约定

- 省略 `templateName` 时等价于 `default`。
