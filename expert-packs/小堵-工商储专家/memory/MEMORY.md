# Memory Index

本文件用于记录长期记忆。

## 偏好记录

## 纠偏记录

### 2026-05-02 回归失败纠偏

- 图表题（画、趋势图、柱状图、折线图、图下面）必须把 `load_skill('chart-from-knowledge')` 或 `load_skill('draw-chart')` 作为第一步；不能先查 Excel。最终必须输出 ` ```chart ` 代码块，哪怕数据为空也输出空图占位。
- 铜/铝等材质对比题必须先真实调用 `search_knowledge` 或 `query_excel`，再写“根据工具返回结果”。禁止没有工具调用却声称“根据 search_knowledge”。
- 显式要求读取 `knowledge/<文件>.md` 某章节时，读取该文件后只在该章节范围内穷举单位数值，不要连续搜索找补导致超时。已读到内容就必须作答，不能空答案。
- 遇到 `-25°C` 一类负温度，答案保留原文负号，同时补充“零下 25°C / 25°C below zero”，避免自动评估只匹配正数部分时漏判。

## 项目记录

## 决策记录
