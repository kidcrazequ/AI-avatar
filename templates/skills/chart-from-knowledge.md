---
name: chart-from-knowledge
description: 当用户需要基于知识库数据画图时，先调用 search_knowledge 或 rag_retrieve 检索相关数据，然后调用 draw-chart 输出 ECharts 图表。
---

# 知识库数据可视化 · chart-from-knowledge

> **级别**：[■■■] 专家
> **版本**：v1.0
> **最后更新**：2026-04-13

---

## 技能说明

这是 `draw-chart` 的高阶版本，负责串联"检索 + 画图"的两步链式调用。
当用户问题涉及**知识库已有数据**的可视化时使用本技能。

## 触发条件

- 用户要求"画图"但数据需要先从知识库检索（没有直接贴数据）
- 用户问题含数据关键词 + 可视化关键词：「今年各站点业绩用图」「过去 3 个月客户数量趋势图」

**触发关键词**：同 `draw-chart` + 任何知识库相关的数据查询词

## 输入

| 参数 | 是否必须 | 说明 |
|------|---------|------|
| 查询语句 | 必须 | 用户问题中的数据主题，用于 search_knowledge 检索 |
| 图表类型 | 可选 | 由 LLM 推断或用户指定 |

## 执行流程（**严格 3 步**）

### Step 1: 检索数据

调用 `search_knowledge` 或等效 RAG 工具，查询相关知识片段。

```
search_knowledge("2024 月度销售")
```

### Step 2: 校验数据可画性

检索回来的结果必须满足：
- 至少 3 个数据点（否则画图无意义）
- 有明确的类目维度（时间、地点、品类等）
- 有明确的数值维度（金额、数量、百分比等）

如果检索无果 → **不要硬画**。按 draw-chart 的"失败回退"章节诚实说明。

### Step 3: 遵循 draw-chart 技能输出

**加载** `draw-chart` 技能（调用 `load_skill('draw-chart')`）然后按其规范输出 ECharts option JSON。

你的最终回答结构：

```
<一两句自然语言解读，指出数据的关键观察>

```chart
{ ...ECharts option... }
```

<可选：1-2 句来源说明，例如"数据来源：月度销售.xlsx">
```

## 输出格式

与 `draw-chart` 完全一致。本技能不额外约束输出，只约束"先检索"这一步。

## 禁止事项

- ❌ 跳过 search_knowledge 直接画图
- ❌ 把"检索无果"偷换成"编造示例"
- ❌ 输出多张图表而不是 1 张（一次对话 1 个图）

## 示例

### 示例：从 Excel 导入的销售数据画趋势图

**用户**：用图表看下 2024 年各月销售额

**你的执行**：
1. `search_knowledge("2024 月度销售")` → 命中 `月度销售.md` 的 `## 2024` section
2. 解析该 section 中的 GFM 表格，提取 12 个月的数字
3. `load_skill('draw-chart')` → 按 UED 规范生成 ECharts option
4. 输出：解读 + ```chart 代码块 + 来源说明

## 引用知识

- 本分身的 `draw-chart.md` 技能（必读，UED 规范在那里）
- 知识库中所有 Excel 导入的 `.md` 文件
